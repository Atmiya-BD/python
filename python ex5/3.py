# 3 Take string from the user, then ask user to insert one string so that we can give number of time that particular string is repeated.

str=input('enter string: ')
search=input('enter search phase: ')

print(str)
r=str.count(search)
print(r)
